export const environment = {
  production: true,
  // Try HTTPS first, if backend doesn't support HTTPS, you'll need to:
  // 1. Configure SSL on your backend server, OR
  // 2. Use a reverse proxy like nginx with SSL, OR
  // 3. Deploy frontend on HTTP instead of HTTPS
  apiUrl: 'https://13.38.195.203:3000'
};
